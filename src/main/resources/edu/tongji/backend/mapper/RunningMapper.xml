<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.tongji.backend.mapper.RunningMapper">
    <select id="getByExerciseIdRunning" resultType="edu.tongji.backend.entity.Running">
        SELECT * FROM running WHERE exercise_id = #{exercise_id}
    </select>
</mapper>