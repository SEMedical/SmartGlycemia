<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.tongji.backend.mapper.HospitalMapper">
    <select id="getAllGEO" resultType="edu.tongji.backend.dto.HospitalDTO">
        SELECT hospital_id as id,latitude,longitude
        FROM hospital;
    </select>
</mapper>