# ... previous config options...

comment:
  layout: "header,reach, diff, flags, components,tree,betaprofiling"  # show component info in the PR comment
component_management:
  default_rules:
    statuses:
      - type: project
        target: auto
        branches:
          - "!main"
  individual_components:
    - component_id: gateway
      name: Gateway
      paths:
        - gateway/**
    - component_id: user-service
      name: User Service
      paths:
        - user-service/**
    - component_id: glycemia-service
      name: Glycemia Service
      paths:
        - glycemia-service/**
    - component_id: interact-service
      name: Interact Service
      paths:
        - interact-service/**
    - component_id: exercise-service
      name: Exercise Service
      paths:
        - exercise-service/**
    - component_id: oa-service
      name: OA Service
      paths:
        - oa-service/**
    - component_id: interact-service
      name: Interact Service
      paths:
        - interact-service/**
